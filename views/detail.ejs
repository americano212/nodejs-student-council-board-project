<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/font.css" />
    <link rel="stylesheet" href="/css/detail.css" />
    <link rel="stylesheet" href="/css/footer.css" />
    <title>서강대학교 공학부 신문고</title>
    <script
      src="https://kit.fontawesome.com/2d323a629b.js"
      crossorigin="anonymous"
    ></script>
    <script src="/js/mobile_menu.js" defer></script>
  </head>
  <body>
    <%- include('header') %>

    <div class="content">
      <div class="title">
        <h2>
          <% if(contents.b_type==1){ %>
              [질문]&nbsp
          <% }else if(contents.b_type==2){ %>
              [건의]&nbsp
          <% }else if(contents.b_type==3){ %>
              [홍보]&nbsp
          <% } %>
          <%= contents.b_title %>
        </h2>
        <div class="linee">
          <span class="hit" style="color:#8889A4">
            <%= contents.b_created %>&nbsp|&nbsp조회수&nbsp<%= contents.b_hit %>
          </span>
          <% if(is_owner){ %>
            <div class="editor">
              <input type="button" value="수정" class="edit" onClick="location.href='/edit/<%= contents.b_seq %>'">
              <input type="button" value="삭제" class="delete" onClick="location.href='/delete/<%= contents.b_seq %>'">
            </div>
          <% }else{ %>
          <% } %>
        </div>

      </div>
      <hr color="#8889A4" size="1px">
      <div class="text_">
        <%- description_ %>
      </div>
      <br>
      <br>
      <br>
      <h2 style="color:#72738A">Comments</h2>
      <hr color="#8889A4" size="1px">
      <div class="reply_box">
          <form method="post" class="reply_create_box">
              <textarea name="reply" id="reply" style="﻿overflow-y:scroll; overflow-x:hidden; resize: none;"></textarea>
              <input type="submit" id="submit">
          </form>
      </div>
      <hr color="#8889A4" size="1px">
      <% replys.forEach(function(value){ %>
          <% if(value.r_status == 1){ %>
              <div class="one_reply_box">
                  <span class="create_reply"><%= value.r_created %> </span>
                  <span style="color:#5E5E72;font-weight:bold;font-size:12px;"> 관리자</span>
                  <p style="color:#5E5E72;font-weight:bold"><%= value.r_content %></p>
                  <hr color="#D2D3F2" size="1px">
              </div>
          <% }else{ %>
              <div class="one_reply_box">
                  <p class="create_reply"><%= value.r_created %></p>
                  <p style="color:#5E5E72"><%= value.r_content %></p>
                  <hr color="#D2D3F2" size="1px">
              </div>
          <% } %>
      <% }); %>
    </div>
    <%- include('footer') %>
  </body>
</html>
