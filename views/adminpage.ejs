<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="stylesheet" href="/css/font.css" />
    <link rel="stylesheet" href="/css/board.css" />
    <title>서강대학교 공학부 신문고</title>
    <script
      src="https://kit.fontawesome.com/2d323a629b.js"
      crossorigin="anonymous"
    ></script>
    <script src="/js/mobile_menu.js" defer></script>
  </head>
  <body>
    <div class="table_box">
        <table>
            <thead>
            <tr>
                <th>번호</th>
                <th>종류</th>
                <th>제목</th>
                <th>만든날짜</th>
                <th>조회수</th>
                <th>글 상태</th>
            </tr>
            </thead>
            <tbody>
            <% contents.forEach(function(value){ %>
                <tr>
                    <% if(value.b_status==4){ %>
                        <td><%= value.b_seq %>[삭제]</td>
                    <% }else{ %>
                        <td><%= value.b_seq %></td>
                    <% } %>
                    <% if(value.b_type==1){ %>
                        <td>[질문]</td>
                    <% }else if(value.b_type==2){ %>
                        <td>[건의]</td>
                    <% }else if(value.b_type==3){ %>
                        <td>[홍보/제휴]</td>
                    <% }else{ %>
                        <td></td>
                    <% } %>

                    <td><a href="/admindetail/<%= value.b_seq %>"><%= value.b_title %></a></td>
                    <a href="/admindetail/<%= value.b_seq %>"><td><%= value.b_created %></td></a>
                    <td><%= value.b_hit %></td>
                    <td><%= value.b_status %></td>
                </tr>
            <% }); %>
            <tbody>
        </table>
    </div>
  </body>
</html>
